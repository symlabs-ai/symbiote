# Symbiote v0.6.2 — Release Report

**Data:** 2026-05-27
**Tipo:** Patch (0.6.1 → 0.6.2) — escopo agregado de MINOR, bumpado como patch por decisão do mantenedor para preservar estabilidade do canal de versionamento
**Para clientes:** hosts embedados (`you_news`, `sym_talk_lt`) e qualquer integração que usa o kernel Symbiote
**CHANGELOG completo:** [`CHANGELOG.md`](../../CHANGELOG.md#v062---2026-05-27)
**Plano de rollout operacional:** [`docs/ROLLOUT.md`](../ROLLOUT.md)

---

## TL;DR

O Symbiote agora tem um **loop completo de self-improvement** — extração de memórias durables via LLM, criação autônoma de skills procedurais, lifecycle automation, e auditoria post-hoc. **Tudo opt-in atrás de duas flags em `EnvironmentConfig`**:

```python
kernel.environment.configure(
    symbiote_id=sym.id,
    reflection_mode="llm",        # default: "keyword" (legacy)
    skill_review_enabled=True,    # default: False
)
```

**Sem essas flags, comportamento é idêntico ao v0.6.1, bit-a-bit.** Clientes embedados (`you_news`, `sym_talk_lt`) podem atualizar sem mudar nenhuma linha de código.

## O que entrou (agregado dos Sprints 1-5)

### Reflection LLM (Sprint 1 + 2)

`ReflectionEngine` ganha 4 modos no `EnvironmentConfig.reflection_mode`:

| Modo | Comportamento |
|---|---|
| `keyword` (default) | Heurística regex/keyword scan da v0.6.1. Zero custo de LLM. |
| `llm` | Engineered prompt (signals + anti-patterns + PATCH>CREATE hierarchy) via `_evolver_llm`. **Requer `kernel.set_evolver_llm(...)` setado**, senão `ValueError` em `configure()`. |
| `llm_main` | Igual a `llm`, mas usa o LLM principal — opt-in explícito ao custo do modelo caro. |
| `hybrid` | Roda os dois caminhos. Persiste o keyword. Loga o diff em `reflection_audit` pra auditoria offline antes de promover pra `llm`. |

**`MemoryPort.update()`** novo: PATCH first-class. LLM pode refinar memórias existentes em vez de duplicar (`action: "patch", target_id: "<uuid>"`). Sem isso, o caminho LLM caía em CREATE silenciosamente.

**`MemoryConsolidator` refatorado**: agora produz apenas 1 `MemoryEntry(type="session_summary")` por overflow, não mais N facts tipados. Elimina redundância com `ReflectionEngine` (ambos extraíam o mesmo via LLM antes).

### Skill self-improvement (Sprints 3, 4, 5)

| Componente | O que faz | Default |
|---|---|---|
| **`SkillsStore`** | CRUD de skills no disco (`create`, `edit`, `patch`, `delete`, `write_file`, `remove_file`). Atomic writes, validação de nome, path-traversal blocked. | Tool registrada, **não autorizada** automaticamente |
| **`skill_manage` tool** | Expõe o `SkillsStore` ao LLM via `ToolGateway`. | Host opta-in via `kernel.environment.configure(tools=[..., 'skill_manage'])` |
| **`BackgroundReviewEngine`** | Daemon thread non-blocking. Roda `SKILL_REVIEW_PROMPT` após cada N tool iterations e em `close_session`, aplica ops via `SkillsStore`. | `skill_review_enabled=False` |
| **`.skill_meta.json` sidecar** | Lifecycle status (quarantine / active / stale / archived), agent_created, pinned, use_count, patch_count. **Skill sem sidecar = humana, intocada pelo loop.** | Ausência preserva todas as skills humanas existentes |
| **Auto-promote** | Quarantine → active após N successful loads (`skill_auto_promote_threshold`, default 3). | 3 (0 desativa) |
| **Auto-archive** | Quarantine sem uso há N dias → archived (`skill_quarantine_timeout_days`, default 14). Roda no `DreamEngine.PrunePhase`. | 14d (0 desativa) |
| **Caps separados** | `max_active_skills=20` (LLM-visible) + `max_quarantine_skills=10` (backlog). | 20 + 10 |

### Auditoria

| Tabela | O que registra | CLI |
|---|---|---|
| `reflection_audit` | Cada `close_session` em modo `hybrid` / `llm` (keyword facts vs LLM facts, erros) | `symbiote audit reflection --days N --diff` |
| `skill_review_audit` | Cada spawn do `BackgroundReviewEngine` (trigger, applied/skipped, ops_json, erro) | `symbiote audit skill-review --days N --ops` |
| **Retention** | Sem retenção automática (audit cresce indefinido) | `symbiote audit prune --days 90 [--table T] [--dry-run]` — rodar via cron semanal |

### Hardening (Sprints 4.1, 4.2, 5.1)

Endereçados no review de cada sprint, todos sem mudança de comportamento default:

- **Threading**: `_background_review_for` lock pra evitar race na construção do engine; `BackgroundReviewEngine.spawn()` deduplica por `session_id` (evita spawn-storm em sessões tool-heavy); `SkillsLoader.refresh()` atomic ref swap (era `dict.clear()` + `_discover` — leitor concorrente caía em `RuntimeError`); per-skill_dir lock em `usage.mark_used` (evita perda de increment em loads concorrentes)
- **Prompts**: `render_prompt` helper substitui `str.format()` para evitar `KeyError` silencioso em schemas JSON literais; PT-BR + EN nos exemplos do `_SIGNALS_BLOCK`; tags enforcement (auto-fill `[type]` em vez de rejeitar entry)
- **Caps**: `max_active_skills` agora conta só active; `max_quarantine_skills` separado (sem deadlock quando backlog enche)
- **Config**: `_int_or_default` helper distingue `None` de `0` no `_row_to_config` (essencial para os fields novos onde `0` é sentinela "desativado")

## Backward-compat — garantias pinadas por teste

3 invariantes verificadas em `tests/unit/test_backward_compat_reflection.py` e `tests/unit/test_skills_loader_lifecycle.py`:

1. **Kernel sem flag = zero LLM calls extras em `close_session`** (defaults: `reflection_mode='keyword'`, `skill_review_enabled=False`)
2. **Skill humana (sem `.skill_meta.json`) = `agent_created=false`, `status='active'`, intocada por Dream**
3. **Todo kwarg novo é kwarg-only com default `None`** — `ChatRunner(background_review=None, skill_nudge_interval=10)`, `ReflectionEngine(llm=None, mode='keyword', storage=None)`

`MemoryPort.update()` é método novo no Protocol — adapters externos que não implementam só falham se `update()` for **chamado** (caminho LLM). Com `reflection_mode='keyword'` default, esses adapters continuam compatíveis.

## Custos

Estimativa com Haiku 4.5 como `_evolver_llm`:

| Operação | Trigger | Custo por evento |
|---|---|---|
| Reflection LLM | `close_session` | ~$0.0075 |
| Skill review nudge | a cada 10 tool iters | ~$0.012 |
| Skill review final | `close_session` | ~$0.014 |
| Compression | `tokens > 2000` mid-session | ~$0.003 |

Single-user típico (5 sessões/dia, 20 tool iters/sessão): **~$1-3/mês** extra com todas as flags ON. Multi-tenant: estimar via `reflection_audit` antes de habilitar globalmente.

## Quickstart — Ativar gradualmente

Recomendação: seguir [`docs/ROLLOUT.md`](../ROLLOUT.md) com 3 estágios em 1 symbiote piloto antes de globalizar. Sketch:

```python
# Pré-requisito sempre
kernel.set_evolver_llm(ForgeLLMAdapter(model="claude-haiku-4-5"))

# Estágio 1 — Reflection em modo hybrid (1-2 semanas)
kernel.environment.configure(symbiote_id=sym.id, reflection_mode="hybrid")
# Auditar:
#   symbiote audit reflection --days 7 --diff

# Estágio 2 — Reflection LLM como persistência real (2 semanas)
kernel.environment.configure(symbiote_id=sym.id, reflection_mode="llm")

# Estágio 3 — Skill review autônomo (após Estágios 1+2 estáveis)
kernel.environment.configure(symbiote_id=sym.id, skill_review_enabled=True)
```

**Kill switch** se algo der errado:

```python
kernel.environment.configure(
    symbiote_id=sym.id,
    reflection_mode="keyword",
    skill_review_enabled=False,
)
# Defaults legados voltam no próximo turn.
```

## Migração da v0.6.1

**Nenhuma ação necessária.** Atualizar a dependência:

```bash
pip install -U symbiote
# ou pra clientes pinados em pyproject.toml:
#   "symbiote @ git+...@v0.6.2"
```

Migrations SQL são idempotentes (5 novas colunas em `environment_configs`, 1 nova coluna em `memory_entries`, 1 tabela `skill_review_audit`). Rodam automaticamente no primeiro `kernel.init_schema()` após o bump.

**Smoke test feito**: `sym_talk_lt` (cliente embedado em v0.6.1) com `v0.6.2` produz **idêntica taxa de testes** (32 fails pré-existentes do próprio sym_talk_lt, zero regressão atribuível ao Symbiote).

`you_news` está pinned em `v0.3.3` — pula 3 majors e tem URL apontando pro GitHub público (já não existe). Atualização requer migração própria do projeto cliente, **fora do escopo deste release**.

## Limitações conhecidas

| Item | Status | Workaround |
|---|---|---|
| **`DreamEngine.ReconcilePhase` para skills** (detectar overlap de `when_to_use`) | Não implementado — esperando volume ≥30 skills agent-created pra calibrar threshold | Manual via `symbiote skills list` + inspeção |
| **Tool loop reentrant** no `BackgroundReviewEngine` (multi-call estilo Hermes) | Não implementado — single-call vai primeiro; promover se qualidade plateaur | Aceitar single-call por enquanto |
| **Embedding-based skill discovery** | Não implementado — cap atual de 30 entries em `_format_existing_skills` cobre até ~50 skills | Sem ação até library passar de 50 |
| **`SQLiteAdapter` single-cursor thread safety** (B-47) | Conhecido, não corrigido. Race em writes concorrentes. Single-user OK; multi-tenant precisa fix antes de paralelizar | Lock externo se necessário; ver B-47 no BACKLOG |
| **4 testes pré-existentes falhos** (B-46) em `test_instant_mode` + `test_generation_settings` | Conhecido, fix trivial pendente | Drift pós-v0.5.0, não-funcional |
| **Auto-archive de quarantine não retira do disco** | Apenas seta `status="archived"`. Filesystem cresce. | `find .symbiote/skills -name '.skill_meta.json' \| xargs grep -l 'archived'` para inventário manual |
| **`reflection_audit` / `skill_review_audit` sem retention** automática | `symbiote audit prune --days 90` resolve, recomenda cron semanal | Documentado em ROLLOUT.md e no `--help` da CLI |

## Onde aprender mais

- **Plano de rollout operacional:** [`docs/ROLLOUT.md`](../ROLLOUT.md) — 3 estágios com critérios mensuráveis e kill switch
- **CHANGELOG:** [`CHANGELOG.md`](../../CHANGELOG.md) — detalhamento por sprint
- **Backlog (bugs documentados):** [`BACKLOG.md`](../../BACKLOG.md) — B-46, B-47
- **Audit logs:**
  - `symbiote audit reflection --days 7 --diff`
  - `symbiote audit skill-review --days 7 --ops`
  - `symbiote audit prune --days 90 --dry-run`
- **Inventário de skills:**
  - `symbiote skills list [--all]`
  - `symbiote skills promote <name>`
  - `symbiote skills pin <name> [--unpin]`

## Reportar problemas

- **Bug em reflection / consolidator / memory** → issue com tag `area:memory`
- **Bug em skills (loader, store, lifecycle)** → tag `area:skills`
- **Bug em audit / dream curator** → tag `area:dream`
- **Performance issue** → tag `area:perf`, mencione tamanho do `memory_entries` e número de skills

---

## Commits agregados no release

Cinco squashes na main consolidando ~30 commits de branches feature/hardening:

| SHA | Mensagem |
|---|---|
| `71006be` | feat(memory+skills): self-improvement loop (Sprints 1-4) (#1) |
| `93c5e6a` | feat(skills): lifecycle automation + skill_review_audit (Sprint 5) (#2) |
| `3d9d635` | chore: release v0.6.2 (CHANGELOG) |
| `5fb6727` | chore: bump version to 0.6.2 (`pyproject.toml` + `__init__.py`) |
| `1ec50b7` | chore: v0.6.2 follow-ups (docs + bugs + retention CLI) (#3) |

**Estatísticas agregadas** vs v0.6.1:

- **~200 testes novos** (suite passa de ~1320 → ~1526)
- **~5800 LoC** adicionadas (incluindo docs ROLLOUT.md e este release report)
- **5 colunas novas** em `environment_configs`, **1 coluna** em `memory_entries`, **1 tabela nova** (`skill_review_audit`) — todas idempotent migrations
- **3 PRs** mergeados (#1, #2, #3)
